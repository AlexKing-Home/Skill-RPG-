# UI parity notes

Approved mobile references are the source of truth for character creation, world map, location map, and character profile. Mobile layout must remain within viewport width, maps must render from local public assets, and interactive controls must remain real React buttons.
